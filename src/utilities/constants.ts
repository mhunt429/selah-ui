import { useBreakpointValue } from "@chakra-ui/react";

// tslint:disable-next-line
const marginTopValue = useBreakpointValue({
  base: "5%",
  md: "8%",
  lg: "5%",
});

export default marginTopValue;
